# 드론 영상 분석 및 객체 추적
>  **드론 영상 파일 또는 스트림을 입력으로 받아 YOLO로 객체 추적**하는 가장 실용적인 파이썬 예제
> 이 코드는 **OpenCV + YOLOv5/YOLOv8 + Python** 기반이며, 드론 영상 분석에 바로 적용할 수 있음

아래 예제는 **YOLO 모델로 객체를 검출하고, 간단한 추적(Tracking)까지 수행**하는 형태

---

# 🚀 **YOLO 기반 드론 영상 객체 추적(Python) 예제**

## ✔ 준비물
- Python 3.x  
- OpenCV  
- Ultralytics YOLO (YOLOv5/YOLOv8)  
- 드론 영상 파일 또는 RTSP/UDP 스트림  

설치:

```bash
pip install ultralytics opencv-python
```

---

# 🟦 **1) YOLO로 드론 영상 객체 추적 기본 코드**

```python
from ultralytics import YOLO
import cv2

# YOLO 모델 로드 (YOLOv8n: 가벼워서 드론 영상에 적합)
model = YOLO("yolov8n.pt")

# 드론 영상 파일 또는 스트림
# cap = cv2.VideoCapture("drone_video.mp4")  # 파일
# cap = cv2.VideoCapture("rtsp://localhost:8554/live")  # RTSP 스트림
cap = cv2.VideoCapture(0)  # 웹캠 테스트용

while True:
    ret, frame = cap.read()
    if not ret:
        break

    # YOLO 추론
    results = model(frame, stream=True)

    # 결과 시각화
    for r in results:
        boxes = r.boxes
        for box in boxes:
            x1, y1, x2, y2 = box.xyxy[0].cpu().numpy()
            cls = int(box.cls[0])
            conf = float(box.conf[0])
            label = model.names[cls]

            # 박스 그리기
            cv2.rectangle(frame, (int(x1), int(y1)), (int(x2), int(y2)), (0, 255, 0), 2)
            cv2.putText(frame, f"{label} {conf:.2f}",
                        (int(x1), int(y1) - 10),
                        cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 255, 0), 2)

    cv2.imshow("Drone Object Tracking (YOLO)", frame)

    if cv2.waitKey(1) == 27:  # ESC 종료
        break

cap.release()
cv2.destroyAllWindows()
```

---

# 🟦 **2) 드론 영상에서 특정 객체만 추적하고 싶다면?**

예: 사람(person)만 추적

```python
TARGET = "person"

for r in results:
    for box in r.boxes:
        cls = int(box.cls[0])
        label = model.names[cls]

        if label != TARGET:
            continue
```

---

# 🟦 **3) 드론 영상에서 객체 추적 + ID 부여(DeepSORT 연동)**

YOLO는 “검출”만 하고, “추적(ID 유지)”은 하지 않아.  
추적까지 하고 싶다면 **DeepSORT**를 붙이면 돼.

아래는 YOLO + DeepSORT 구조 예시:

```python
from ultralytics import YOLO
from deep_sort_realtime.deepsort_tracker import DeepSort
import cv2

model = YOLO("yolov8n.pt")
tracker = DeepSort(max_age=30)

cap = cv2.VideoCapture("drone_video.mp4")

while True:
    ret, frame = cap.read()
    if not ret:
        break

    results = model(frame)[0]

    detections = []
    for box in results.boxes:
        x1, y1, x2, y2 = box.xyxy[0].cpu().numpy()
        conf = float(box.conf[0])
        cls = int(box.cls[0])
        label = model.names[cls]

        detections.append(([x1, y1, x2 - x1, y2 - y1], conf, label))

    tracks = tracker.update_tracks(detections, frame=frame)

    for track in tracks:
        if not track.is_confirmed():
            continue

        track_id = track.track_id
        l, t, r, b = track.to_ltrb()

        cv2.rectangle(frame, (int(l), int(t)), (int(r), int(b)), (255, 0, 0), 2)
        cv2.putText(frame, f"ID {track_id}",
                    (int(l), int(t) - 10),
                    cv2.FONT_HERSHEY_SIMPLEX, 0.6, (255, 0, 0), 2)

    cv2.imshow("YOLO + DeepSORT Drone Tracking", frame)
    if cv2.waitKey(1) == 27:
        break
```

---

# 🎯 **이 예제로 할 수 있는 것**

- 드론 영상에서 사람/차량/배 등 객체 검출  
- 드론이 이동해도 객체를 계속 추적  
- 객체별 ID 부여  
- 실시간 스트림 분석  
- YOLOv8 기반 고속 추론  

---

# 📌 jong에게 추천하는 드론 영상 테스트 방법

드론이 없어도 아래처럼 테스트 가능해:

- Pexels, Pixabay 등에서 드론 영상 다운로드  
- FFmpeg로 가짜 RTSP 스트림 생성  
- AirSim 시뮬레이터에서 드론 카메라 영상 받기  

원하면 **가짜 드론 RTSP 서버 만드는 코드**도 만들어줄게.

---

jong, 다음 중 어떤 걸 더 해보고 싶어?

- YOLO로 사람/차량만 추적하는 버전  
- 드론 영상에서 객체 속도 계산  
- 드론 영상에서 객체 거리 추정  
- YOLO + 경로 예측(칼만 필터)  
- YOLO + AirSim 연동  

원하는 방향으로 더 확장해줄게.
